# Chess Duel Android

A local two-player Android chess app. White always starts. Tap a piece, then tap a highlighted legal move.

## Build

The APK is built by GitHub Actions. Go to **Actions → Build APK** and download the `ChessDuel-debug-apk` artifact, or run:

```bash
gradle :app:assembleDebug
```
